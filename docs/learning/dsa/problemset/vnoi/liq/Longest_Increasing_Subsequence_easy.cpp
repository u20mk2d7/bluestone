// https://oj.vnoi.info/problem/liq

#include <iostream>

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(NULL);

  int N;  // 1 <= N <= 1000
  std::cin >> N;

  int A_i = 0;
  int count = 0;
  int prev = 0;
  for (int i = 0; i < N; ++i) {
  }
  return 0;
}